# Pumpkinpipe - Planning

## Overview

Plan something phenomenal using the Pumpkinpipe library and Python. Your project will need to be planned, tested, and presented to the class to demonstrate your understanding of structured programming. Be sure to focus on using `selection` and `iteration` structures to make your project work.

## Project Criteria

Your task is to:

* Plan a project using the Pumpkinpipe library and Python.
* Identify values that can be used from the hand detection that could be used in your project. These inputs might include but are not limited to:
    * Handedness (right or left)
    * Number of hands
    * Hand position (x, y, z)
    * Landmark positions
    * Finger flags / fingers up / fingers down
    * Distance between landmarks
* Identify outputs that could come from processing the above input data. This could look like:
    * Rendering shapes / text / lines
    * Adding sounds that play
    * Integrating other libraries to visualize / control other things
    * Creating a simple game loop with score / points

Show your planning with the following included in your planning document:

* Overview outlining the idea for your project. The overview should be between 50-75 words.
* Use IPO charts to break down the input, process steps, and output for different modules in your project.
* Use pseudocode or a structured flow chart to communicate the flow of your overall program.

## Submission

* *5% will be deducted for naming files incorrectly.*
* *5% will be deducted for submitting files of the wrong type or missing specified file types.*
* *5% will be deducted for files being located in the wrong folders.*

### Planning Document

* **File Type:** `.md`
* **File Name:** `student planning.md`.
* **File Location:** `Project Documents` folder.
* **Push your project folder to GitHub Classroom**

## Mastery Rubric
| Outcome                                                                            | **5 – Mastery of Skill**                                                                                                                                                                                        | **4 – Excellence in Skill**                                                                | **3 – Proficiency in Skill**                                             | **2 – Attempting Skill**                                | **1 – Starting to Attempt Skill**                     | **0 – Refusal to Engage with Skill** |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------- | ----------------------------------------------------- | ------------------------------------ |
| **1.1 Determine whether the problem can be solved with an IPO model**              | Planning clearly and completely uses the IPO model to represent how hand detection inputs (e.g., landmarks, fingers, position) are processed into meaningful outputs (e.g., visuals, interactions, game logic). | Planning clearly uses the IPO model to represent how hand data is processed into outputs.  | Correctly applies the IPO model to the hand-based project.               | Attempts to use IPO but shows partial misunderstanding. | IPO use is unclear or incorrect.                      | Makes no attempt to use IPO.         |
| **1.2 Determine if the problem contains more than one IPO module**                 | Project is broken into a comprehensive set of IPO modules that clearly represent all major features (e.g., detection, processing, output rendering, interaction/game logic) with no overlap.                    | Project is broken into well-organized IPO modules that represent all major features.       | Project is broken into multiple IPO modules that represent key features. | Attempts to identify modules but misses key components. | Treats the project as a single module when it is not. | No attempt to identify modules.      |
| **1.3 Break the problem into smaller modules and identify the IPO model for each** | Modules are clearly defined, reusable, and directly aligned with hand detection features (e.g., gesture recognition, distance checks, drawing); IPO for each is precise and complete.                           | Modules are logically separated and aligned with project features; IPO is clearly defined. | Modules are clearly identified with accurate IPO breakdowns.             | Modules are incomplete or inconsistently defined.       | Modules are poorly defined or incorrect.              | No modular breakdown present.        |
| **1.4 Identify internal data and external input for each IPO module**              | All inputs (e.g., hand landmarks, finger states, distances) and internal variables are clearly identified, categorized, and used intentionally within the project logic.                                        | Inputs and internal data are clearly identified and logically grouped.                     | Correctly identifies hand data inputs and internal variables.            | Confuses internal data with external inputs.            | Minimal or incorrect identification of inputs.        | No identification of data sources.   |
| **1.5 Create processing steps using appropriate control structures**               | Processing steps are clearly defined, optimized, and directly mapped to selection and iteration (e.g., loops over hands, conditionals for gestures); logic is implementation-ready.                             | Processing steps are clear and correctly use selection and iteration.                      | Processing steps are written and use appropriate control structures.     | Processing steps are vague or partially incorrect.      | Processing steps are largely missing or incorrect.    | No processing steps identified.      |
| **1.6 Use common algorithm patterns when needed**                                  | Algorithm patterns (e.g., counting fingers, comparing distances, state tracking) are used strategically and adapted effectively to the hand-based interaction.                                                  | Algorithm patterns are used intentionally and appropriately across modules.                | Appropriate algorithm patterns are correctly applied.                    | Attempts patterns but applies them incorrectly.         | Little evidence of algorithmic patterns.              | No algorithm patterns used.          |
| **1.7 Identify the required outputs for each IPO module**                          | Outputs are fully defined and directly tied to hand interaction results (e.g., drawing changes, feedback, game state, visuals, or sounds).                                                                      | Outputs are clearly defined and connected to hand-based interactions.                      | Correctly identifies required outputs for each module.                   | Outputs are incomplete or inconsistently identified.    | Outputs are unclear or incorrect.                     | No outputs identified.               |
| **1.8 Arrange IPO elements in a logical order**                                    | IPO sequencing reflects a real-time system (capture → detect → process → output) and ensures smooth interaction and correct program flow.                                                                       | IPO elements are ordered logically to support correct processing and interaction.          | IPO elements are arranged logically.                                     | IPO order is inconsistent or partially incorrect.       | IPO order is largely incorrect.                       | No logical ordering present.         |
| **1.9 Write the algorithm in a clear, standard format**                            | Algorithm (pseudocode/flowchart) clearly represents the full hand-tracking workflow and could be directly implemented with minimal changes.                                                                     | Algorithm is clear, structured, and closely aligned with implementation.                   | Algorithm is clearly written using standard format.                      | Algorithm is difficult to follow or incomplete.         | Algorithm format is unclear or incorrect.             | No algorithm provided.               |


## Outcome Criteria

* **1 Demonstrate the ability to plan for structured programming challenges that incorporate selection and iteration structures**
    * **1.1** Determine whether a problem can be solved with an algorithm using the input–process–output (IPO) model.
    * **1.2** Decide if the problem contains more than one IPO module.
    * **1.3** Break the problem into smaller modules and identify the input, processing, and output for each.
    * **1.4** Identify what data the program already has, what data the user must provide, and group those inputs logically using suitable control structures.
    * **1.5** Identify the processing steps needed for the solution and group them logically using suitable control structures.
    * **1.6** Use common algorithm patterns when needed, such as accumulation or finding maximum/minimum values.
    * **1.7** Identify the required outputs and group them logically using suitable control structures.
    * **1.8** Arrange all inputs, processes, and outputs in a logical order so that processing only happens when all needed data is available, and output only happens after processing is complete.
    * **1.9** Write the final algorithm in a clear, standard format (e.g., pseudocode or a structured chart).

